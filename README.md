# text-2-sql-gemini-pro-llm
This project leverages the Google Gemini Pro API to convert natural language questions into SQL queries. The generated queries are executed on a connected database to retrieve and return the relevant results — enabling seamless, conversational access to structured data.
